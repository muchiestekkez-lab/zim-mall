{
  "buildCommand": "prisma generate && next build",
  "framework": "nextjs",
  "regions": ["iad1", "fra1", "cdg1"],
  "crons": [
    {
      "path": "/api/keepalive",
      "schedule": "*/5 * * * *"
    }
  ]
}
